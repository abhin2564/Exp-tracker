{
  "name": "expense-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Local full-stack expense tracker (Node + Express + vanilla HTML/CSS/JS)",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "nodemon": "^3.1.0"
  }
}
